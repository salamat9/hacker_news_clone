setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  images: web
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn hacker_news.wsgi
